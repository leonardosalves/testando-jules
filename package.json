{
  "name": "moisauto-upload-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker for image uploads to R2",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test-upload.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "wrangler": "^3.0.0"
  }
}