# Palette's Journal

## 2025-05-14 - [Initial Entry]
**Learning:** Accessibility should be considered from the start, especially for interactive elements like mobile menus that are often overlooked.
**Action:** Always use `<button>` for interactive elements instead of `<div>` or `<span>` to ensure keyboard accessibility.
